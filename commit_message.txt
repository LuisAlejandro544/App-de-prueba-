feat: Animaciones morfológicas en bucle para conversión de audio y extracción de video

- Implementado componente de animación fluida en Jetpack Compose (`MorphingConversionAnimation.kt`):
  * `VideoToAudioMorphAnimation`: Animación en bucle con rotación elástica y transición suave de icono de Cámara/Video -> destellos de transmutación -> Nota Musical/Audio con barras de ecualizador dinámicas.
  * `AudioToAudioMorphAnimation`: Animación en bucle con gradiente cónico giratorio, escala pulsante y espectro de frecuencias animado.
- Reemplazado el indicador circular tradicional por las animaciones en los diálogos de progreso:
  * `VideoExtractionProgressDialog.kt`: Integrado `VideoToAudioMorphAnimation`.
  * `ConversionProgressDialog.kt`: Integrado `AudioToAudioMorphAnimation`.
- Optimización y compilación nativa C++ (NDK/CMake) para procesamiento de audio:
  * Motor de procesamiento DSP nativo en C++ (`libnative_audio_engine.so`) con ganancia, mezcla de canales y empaquetador WAV RIFF.
  * Enlace JNI en `NativeAudioBridge.kt` para ejecución de alta fidelidad.
  * Compatibilidad multi-arquitectura validada para `arm64-v8a`, `armeabi-v7a`, `x86` y `x86_64`.
- Automatizado el flujo de GitHub Actions (`commit-changes.yml`) para sincronización continua con cada push.
