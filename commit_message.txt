feat: Implementación de Selector de Vista en el Menú Principal (Cuadrícula, Lista Compacta y Detallada)

- **Selector de Vista Interactivo (`HubViewModeSelector.kt` & `HubModels.kt`)**:
  * Creación del componente de alternancia rápida estilo segmented control en la barra de herramientas.
  * Tres modos de visualización seleccionables:
    - **Cuadrícula (Grid)**: Tarjetas visuales de 2 columnas altamente compactas, mostrando 4 a 6 herramientas en pantalla sin scroll excesivo.
    - **Lista Compacta (List)**: Filas horizontales delgadas estilo explorador/ajustes con navegación directa.
    - **Detallada (Detailed)**: Tarjetas expandidas con explicaciones completas, etiquetas técnicas y botón de acción.
  * Persistencia del estado seleccionado mediante `rememberSaveable`.

- **Componentes Modulares de Tarjetas (`ActiveToolCard.kt`)**:
  * `ActiveToolGridCard`: Tarjeta optimizada para cuadrícula de 2 columnas con icono en gradiente, títulos de tamaño balanceado, badge destacado y área táctil completa de acceso directo.
  * `ActiveToolListRow`: Fila horizontal delgada y limpia con indicador visual y chevron de navegación.
  * `ActiveToolCard`: Mantenimiento de la tarjeta detallada original con tags y flujo completo.

- **Integración y Arquitectura (`MainHubScreen.kt` & `STRUCTURE.md`)**:
  * Refactorización del layout principal para soportar renderizado dinámico en cuadrícula emparejada (`chunked(2)`) y listas sin colisiones de scroll.
  * Actualización de la documentación arquitectónica en `STRUCTURE.md`.
